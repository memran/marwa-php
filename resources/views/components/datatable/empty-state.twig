<div class="mt-5">
  {% include '@Shared/components/empty-state.twig' with {
    state: {
      title: empty_state.title,
      description: empty_state.message,
      icon: empty_state.icon|default('inbox'),
      tone: empty_state.tone|default('muted'),
      size: 'compact',
      surface: 'dashed',
      action: empty_state.action|default(null),
      secondary_action: empty_state.secondary_action|default(null)
    }
  } only %}
</div>
