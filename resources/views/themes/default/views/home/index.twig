{% extends "layout.twig" %}

{% block title %}Home · Default Theme{% endblock %}

{% block content %}
    <div class="space-y-6">
        <h1 class="text-2xl font-bold text-gray-900">
            Welcome to Default Theme
        </h1>

        <p class="text-gray-700 leading-relaxed">
            This is the default look. Clean, light, neutral.
        </p>

        <div class="rounded-lg border bg-white p-4 shadow-sm">
            <div class="text-sm text-gray-500">Debug info:</div>
            <div class="text-xs font-mono text-gray-800">
                Theme: {{ _theme_name }}<br/>
                Chain: {{ _theme_chain|join(' -> ') }}
            </div>
        </div>
    </div>
{% endblock %}
