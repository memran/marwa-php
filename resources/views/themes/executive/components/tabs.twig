{% set tabs = tabs|default({}) %}

{% include '@Shared/components/tabs.twig' with {
  items: tabs.items|default([]),
  active_id: tabs.active_id|default(''),
  label: tabs.label|default('Sections'),
  managed: tabs.managed is defined ? tabs.managed : true,
  shell_class: tabs.shell_class|default('')
} only %}
