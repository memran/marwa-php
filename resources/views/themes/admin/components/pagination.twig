{% include '@Shared/components/pagination.twig' with {
  pagination: pagination|default(null),
  label: label|default('Pagination'),
  show_summary: show_summary is defined ? show_summary : true,
  show_edges: show_edges is defined ? show_edges : true
} only %}
