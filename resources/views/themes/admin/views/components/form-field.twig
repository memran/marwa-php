<div class="grid gap-2">
  <label class="text-sm font-semibold text-slate-200" for="{{ id }}">{{ label }}</label>
  {% if hint is defined and hint %}
    <p class="text-sm text-slate-400">{{ hint }}</p>
  {% endif %}

  {% if type|default('text') == 'textarea' %}
    <textarea
      class="w-full rounded-2xl border border-white/10 bg-white/5 px-4 py-3 text-slate-100 placeholder:text-slate-500 outline-none transition focus:border-cyan-400 focus:ring-4 focus:ring-cyan-400/10 disabled:cursor-not-allowed disabled:opacity-60"
      id="{{ id }}"
      name="{{ name|default(id) }}"
      rows="{{ rows|default(4) }}"
      {% if placeholder is defined and placeholder %}placeholder="{{ placeholder }}"{% endif %}
      {% if disabled|default(false) %}disabled{% endif %}
    >{{ value|default('') }}</textarea>
  {% else %}
    <input
      class="w-full rounded-2xl border border-white/10 bg-white/5 px-4 py-3 text-slate-100 placeholder:text-slate-500 outline-none transition focus:border-cyan-400 focus:ring-4 focus:ring-cyan-400/10 disabled:cursor-not-allowed disabled:opacity-60"
      id="{{ id }}"
      name="{{ name|default(id) }}"
      type="{{ type|default('text') }}"
      value="{{ value|default('') }}"
      {% if placeholder is defined and placeholder %}placeholder="{{ placeholder }}"{% endif %}
      {% if autocomplete is defined and autocomplete %}autocomplete="{{ autocomplete }}"{% endif %}
      {% if disabled|default(false) %}disabled{% endif %}
    >
  {% endif %}

  {% if error is defined and error %}
    <p class="text-sm text-rose-300">{{ error }}</p>
  {% endif %}

  {% if success is defined and success %}
    <p class="text-sm text-emerald-300">{{ success }}</p>
  {% endif %}
</div>
