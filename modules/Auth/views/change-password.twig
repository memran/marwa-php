{% extends '@auth/admin/layout.twig' %}

{% block title %}Change password{% endblock %}

{% block body %}
  <section class="panel">
    <div class="card-heading">
      <p class="eyebrow">Security</p>
      <h2>Change your password</h2>
      <p>Keep your starter account protected with a quick password update.</p>
    </div>

    <form method="post" action="/admin/password/change" class="stack">
      <input type="hidden" name="_token" value="{{ csrf|default('') }}">

      <label class="field">
        <span>Current password</span>
        <input type="password" name="current_password" autocomplete="current-password" required>
      </label>

      <label class="field">
        <span>New password</span>
        <input type="password" name="password" autocomplete="new-password" required>
      </label>

      <label class="field">
        <span>Confirm password</span>
        <input type="password" name="password_confirmation" autocomplete="new-password" required>
      </label>

      <div class="actions">
        <button type="submit" class="button button--primary">Save password</button>
        <a href="/admin/profile" class="button button--ghost">Back to profile</a>
      </div>
    </form>
  </section>
{% endblock %}
